/* ==========================================================================
=============================================================================
		extern
=============================================================================
========================================================================== */
void compiler_main( UCHAR*,UCHAR*,int );				// compiler main
void err_compiler( int );								// line メッセージを表示
void err_next_compiler( void );							// 次のメッセージ
void err_exit_compiler( void );							// error exit
